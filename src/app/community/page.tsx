import { FeedPage } from "@/components/community/FeedPage";

export const metadata = {
  title: "Community",
  description:
    "The FitNear community feed — wins, classes, tips and open slots posted by verified trainers across the UAE.",
};

export default function CommunityPage() {
  return <FeedPage />;
}
